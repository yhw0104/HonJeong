# Java / Gradle
backend/.gradle/
backend/build/
backend/data/
*.log

# OS
.DS_Store

# IDE
.idea/
*.iml
.vscode/

# Office 임시/잠금 파일 (예: ~$문서.xlsx)
~$*

# 설계 문서 — git 추적 제외(로컬 source-of-truth로만 유지)
docs/

# 비밀값(API 키·시크릿) — 절대 커밋 금지. .env.example만 추적한다.
.env
.env.*
!.env.example

# DB 덤프 산출물(대용량) — 서버로 직접 전송한다. 저장소에 올리지 않는다.
backend/scripts/*.sql.gz
